using UnityEngine;
using System.Collections;
using System.Collections.Generic;






public class Communicator : MonoBehaviour {
    public static ServerSnapshot serverSnapshots = new ServerSnapshot();
    public static ClientSnapshot clientSnapshots = new ClientSnapshot();

	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void FixedUpdate () {
	
	}
}